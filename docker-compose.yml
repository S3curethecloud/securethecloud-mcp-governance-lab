services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - LAB_MODE=true

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
